# Render supplies PORT automatically. Use this only for a local deployment command.
PORT=8000

# Free key from https://console.groq.com — powers the Insight Agent's LLM reasoning.
# If unset, the Insight Agent falls back to deterministic stat summaries (no crash, just less interesting output).
GROQ_API_KEY=
